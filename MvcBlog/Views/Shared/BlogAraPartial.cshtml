@{ Layout = null; }
<h5 class="card-header">Search</h5>
@using (Html.BeginForm("BlogAra", "Home", FormMethod.Post,new {enctype="multipart/form-data" }))
{
    <div class="card-body">
        <div class="input-group">
            <input type="text" id="ara" class="form-control" placeholder="Search for..." name="ara">
            <span class="input-group-btn">
                <button class="btn btn-secondary" value="ara" type="submit">Go!</button>
            </span>
        </div>
    </div>
}
